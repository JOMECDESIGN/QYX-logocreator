# Core (SVG -> PNG conversion)
cairosvg>=2.7.0
Pillow>=10.0.0
python-dotenv>=1.0.0

# AI showcase providers (install only what you use)
google-genai>=0.2.0     # IMAGE_PROVIDER=gemini
together>=1.3.0         # IMAGE_PROVIDER=together
openai>=1.40.0          # IMAGE_PROVIDER=openai-compatible (Jimeng/Tongyi/etc.)
requests>=2.31.0
